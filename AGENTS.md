# 仓库代理规则

每次修改代码、配置、测试或文档时，必须在同一次变更中更新仓库根目录的
`CHANGELOG.md`。

- 使用 `## YYYY-MM-DD` 标题，不记录具体时刻。
- 同一天的工作追加到已有日期条目中。
- 记录行为变化和实际执行的验证。
- 持久性的仓库结论放在 `### 分析` 小节中。
- 不得记录 API 密钥、访问令牌、密码或其他秘密信息。

# 变更日志语言

- `CHANGELOG.md` 的标题、变更说明、分析、验证、方案和 TODO 必须使用中文。
- 代码标识符、文件路径、命令、模型名、产品名和原始错误信息可以保留原文。
- 修改历史条目时不得重新引入英文叙述性文字。

# 方案记录

- 每个方案实施后，必须把方案及其实际结果保存到 `CHANGELOG.md`。
- 方案必须使用中文 Markdown。
- 修复已登记问题时，必须注明关联的问题编号、验证证据以及“未解决、部分解决、已解决或不采纳”的状态。

