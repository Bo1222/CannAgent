# M1 Offline Knowledge Snapshot

## TODO

- [x] Add immutable staging/validate/publish snapshot construction.
- [x] Add API, failure, and pattern card schemas.
- [x] Require evidence, applicability, source hash, and valid source sections.
- [x] Report conflicts only for identical API/context keys.
- [x] Build exact symbol indexes and snapshot manifests.
- [x] Add the offline snapshot CLI.
- [x] Verify DataCopy, DataCopyPad, TPipe, and TQue availability.
- [x] Run M1 and repository regressions.

## Added structure

```text
ascendc_multi_turn/knowledge_v2/
├── build.py
├── snapshot.py
├── validate.py
└── tests/test_m1.py

knowledge_store/cann/<version>/snapshots/<snapshot_id>/
├── raw/
├── normalized/
├── facts/
├── cards/
├── indexes/
├── build_manifest.json
└── validation_report.json
```

## Changes

- Snapshot IDs derive from canonical source hashes and compiler/schema versions.
- Repeated identical builds reuse the immutable published snapshot.
- Context keys prevent same-named parameters in different tables/overloads from being merged.
- Symbol discovery tolerates official Markdown exports that squash declarations such as `TPipe pipe` into `TPipepipe` without changing the Raw evidence.
- The phase-scoped CLI is `python -m ascendc_multi_turn.knowledge_v2.build`; the public runtime alias is deferred until the router package integration phase to avoid modifying legacy runtime in M1.

## Validation

- Tests cover immutable rebuilds, snapshot loading, provenance validation, required API symbols, and real-vs-context conflict behavior.
