# M3 Structured Failure Pipeline

## TODO

- [x] Add the `StructuredFailure` schema.
- [x] Parse MTE, AI Core, ACL, compile, source, and generic runtime failures.
- [x] Capture runtime code, subsystem, device exception, core/block IDs, sub-error type, case info, and related symbols.
- [x] Persist structured failure data in evaluation and trajectory records.
- [x] Feed StructuredFailure into StructuredKnowledgeRouter.
- [x] Remove local `details_path` from compact LLM evidence.
- [x] Preserve full local logs and summary audit paths.
- [x] Run M3 and regression tests.

## Added structure

```text
EvalResult.structured_failure
trajectory.rounds[].structured_failure
KnowledgeContext.failure -> StructuredFailure
```

## Changes

- Deterministic diagnostic parsing runs at evaluator failure creation and normalizes custom evaluator failures before settlement.
- Structured routing and PLAN/DIAGNOSE receive explicit fields instead of relying on inaccessible log paths.
- Existing full logs, excerpts, fingerprints, and final audit links remain available locally.

## Validation

- Tests cover MTE illegal configuration, AI Core metadata, ACL errors, compiler symbols, and details-path isolation.
